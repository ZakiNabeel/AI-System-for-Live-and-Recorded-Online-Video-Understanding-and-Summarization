[pytest]
pythonpath = .
markers =
    network: tests that download data from the network
    live: tests that capture live stream chunks with ffmpeg
    e2e: end-to-end tests requiring network access and API keys

addopts = -m "not e2e"
